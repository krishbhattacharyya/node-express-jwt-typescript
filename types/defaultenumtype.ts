enum RoleType {
  anonymous = 'anonymous',
  mediator = 'mediator',
  admin = 'admin'
}

enum ToDoStatusType {
  pending = 'pending',
  progress = 'progress',
  done = 'done',
  notDone = 'not done'
}

export { RoleType, ToDoStatusType }
